{% extends 'base.html.twig' %}

{% block title %}Neues Projekt - {{ organization.name }}{% endblock %}

{% block body %}
<div class="max-w-2xl mx-auto">
    <div class="mb-6">
        <a href="{{ path('app_organization_show', {slug: organization.slug}) }}" class="text-indigo-600 hover:text-indigo-800 text-sm">&larr; {{ organization.name }}</a>
        <h1 class="text-2xl font-bold text-gray-900 mt-2">Neues Projekt erstellen</h1>
    </div>
    <div class="card p-6">
        {{ form_start(form, {attr: {class: 'space-y-5'}}) }}
            <div>
                {{ form_label(form.name, null, {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(form.name, {attr: {class: 'form-input', placeholder: 'Projektname'}}) }}
                {{ form_errors(form.name) }}
            </div>
            <div>
                {{ form_label(form.description, null, {label_attr: {class: 'form-label'}}) }}
                {{ form_widget(form.description, {attr: {class: 'form-input', placeholder: 'Optionale Beschreibung...'}}) }}
                {{ form_errors(form.description) }}
            </div>
            <div class="flex items-start">
                {{ form_widget(form.isPublic, {attr: {class: 'mt-1 rounded border-gray-300'}}) }}
                <div class="ml-3">
                    {{ form_label(form.isPublic, null, {label_attr: {class: 'text-sm font-medium text-gray-700 cursor-pointer'}}) }}
                    <p class="text-xs text-gray-500 mt-0.5">Alle Organisationsmitglieder konnen dieses Projekt sehen und nutzen.</p>
                    {{ form_errors(form.isPublic) }}
                </div>
            </div>
            <div class="flex space-x-3 pt-2">
                <button type="submit" class="btn-primary">Projekt erstellen</button>
                <a href="{{ path('app_organization_show', {slug: organization.slug}) }}" class="btn-secondary">Abbrechen</a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
